# Quafu 真机验证与结果边界

验证日期：2026-07-31  
芯片：Dongling  
提交方式：QuarkStudio `Task`  
编译器：qsteed

## 连通性

只读队列查询成功，Dongling 当时排队数为 0。最小 Bell 电路任务：

- task ID：`2607310921516006475`
- shots：1024
- 状态：Finished
- counts：`00=426, 11=339, 01=161, 10=98`
- `00 + 11` 相关结果比例：74.7%

这证明 token、网络、任务提交和结果读取链路可用，不代表器件保真度的正式标定。

## 当前自适应配对消融复验

当前代码版本在种子 2026、18 客户、4 车辆、容量 28 上完成 20 轮：

- 20/20 轮为 `quafu_hardware`；
- 20 个唯一 task ID；
- 每轮都有完整 counts，且 shots 总数均为 1024；
- `sub_k=5`：1 轮；
- `sub_k=6`：19 轮；
- 18 轮接受；
- 路线代理距离：94.353616 → 92.977571；
- 最终 OR-Tools 距离：91.308691；
- 最大载荷 24，不超过容量 28；
- 18 个客户各出现且只出现一次；
- 每轮 `sub_cnot = 2 × n_edges <= 20`。

task ID 从 `2607311021362682459` 开始，最后一项为 `2607311023065462166`；完整 20 项列表、QASM、counts 和逐轮字段保存在 `results/adaptive_hardware_paired20_v2.json`，原始任务返回保存在 `results/adaptive_hardware_paired20_v2_raw_counts/`。

分层配对结果：

| `sub_k` | N | B 平均改善 | A 平均改善 | 平均 `B − A` |
|---:|---:|---:|---:|---:|
| 5 | 1 | -7.294040 | -1.699949 | -5.594091 |
| 6 | 19 | 0.421853 | 4.883447 | -4.461593 |

该样本的 `B − A` 为负，不能宣称量子优势。5 比特层只有一轮，尤其不能外推。结果仅证明当前版本完成了真实硬件闭环、严格配对记录和自适应分层统计。

## 自适应历史运行

种子 2026、18 客户、4 车辆、容量 28、20 轮：

- 20/20 轮获得真机 counts；
- `sub_k=4`：1 轮；
- `sub_k=5`：3 轮；
- `sub_k=6`：16 轮；
- 最终 OR-Tools 距离：92.302949；
- 最大载荷不超过 28。

该运行发生在逐轮配对影子字段加入之前，因此只能作为真机闭环和自适应规模分布证据，不能直接生成当前版本的 `B − A` 分层表。

## 固定 6 比特历史审计

另有一组曾通过过滤车辆对得到 20/20 轮 6 比特的运行：

- 20 个唯一真机 task ID；
- 每轮 10 条边、20 个 CNOT、1024 shots；
- 路线代理距离：94.353616 → 83.777787；
- 最终 OR-Tools 距离：83.351574；
- 16 轮接受。

这组结果改变了车辆对邻域，存在选择偏差。它只能标记为“固定预算历史实验”，不能与自适应运行直接归因比较，也不是当前算法默认行为。

## 已知对照

同一基础实例的历史结果：

| 方法 | 最终距离 |
|---|---:|
| 容量聚类 + OR-Tools，Tabu=0 | 92.302949 |
| 本地精确 Max-Cut Tabu（历史轨迹） | 90.485907 |
| 自适应真机历史轨迹 | 92.302949 |
| 固定 6 比特真机历史轨迹 | 83.351574 |
| 原 BQM 基线 | 134.118038 |

这些是单一种子、且部分轨迹的邻域策略不同。不得据此宣称量子优势。当前自适应配对复验已单独列在上方，不应与固定 6 比特历史轨迹混合统计。
